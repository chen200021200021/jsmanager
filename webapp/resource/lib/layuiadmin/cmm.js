layui.config({
	base : '../../resource/lib/layuiadmin/' // 静态资源所在路径
});

/*.extend({
	index : 'lib/index' // 主入口模块
})
*/

/*layui.use(['jquery'], function(){
	var $ = layui.jquery;
	$(function() {
		$(this).cmm_code();
		$(this).cmm_category();
	});
});*/